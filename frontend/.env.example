# .env.example
# VITE_API_URL points to the deployed backend. When running locally you can leave it blank to hit localhost.
VITE_API_URL=https://your-render-backend-url.onrender.com

# Builder IO public key (optional, used if UI uses builder)
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
